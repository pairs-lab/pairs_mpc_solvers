# pairs_mpc_solvers

PAIRS MPC solver libraries (CVXGEN) for controllers/trackers — rename-port of CTU-MRS mrs_mpc_solvers.
C++ namespace `mrs_mpc_solvers::` is preserved (ABI), package/include path renamed to `pairs_mpc_solvers`.

Maintainer: Thanh Nguyen Canh <canhthanh@vnu.edu.vn>
