# pairs_mpc_solvers

PAIRS MPC solver libraries (CVXGEN) for controllers/trackers. Compiled from
source — provides the `pairs_mpc_solvers::` C++ namespace and the
`PairsMpcSolvers_MpcTrackerSolver` / `PairsMpcSolvers_MpcControllerSolver`
shared libraries.