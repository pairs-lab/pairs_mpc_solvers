# pairs_mpc_solvers

PAIRS MPC solver libraries (CVXGEN) for controllers/trackers.
The C++ namespace `mrs_mpc_solvers::` is preserved (ABI), while the package and
include path are renamed to `pairs_mpc_solvers`.